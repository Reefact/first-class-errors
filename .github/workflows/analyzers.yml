name: analyzers

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

# Cancel superseded runs on the same branch / PR.
concurrency:
  group: analyzers-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least privilege: this workflow only checks out and builds, so the token
# needs nothing beyond read access to the repository contents.
permissions:
  contents: read

env:
  DOTNET_NOLOGO: 'true'
  DOTNET_CLI_TELEMETRY_OPTOUT: 'true'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 'true'

jobs:
  dogfood:
    name: Dogfood analyzers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          dotnet-version: '10.0.x'

      # Dogfood: build the sample with the analyzers wired in (FirstClassErrors.Usage.csproj references
      # them with OutputItemType=Analyzer). Fails the build on any Error-severity FCExxx diagnostic and
      # surfaces the warnings in the log. The analyzer unit-test suite itself runs as part of the ci
      # workflow, which builds and tests the whole solution.
      - name: Build usage (dogfood analyzers)
        run: dotnet build FirstClassErrors.Usage/FirstClassErrors.Usage.csproj -c Release

  floor:
    name: Dogfood analyzers on the Roslyn floor
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup floor .NET SDK
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          # Exactly 8.0.100 (Roslyn 4.8.0 == VS 2022 17.8), the oldest supported host. A wildcard like
          # 8.0.x would install a later feature band whose bundled Roslyn is newer than the floor, so the
          # job would stop testing the actual floor. Paired with rollForward: disable in the nested global.json.
          dotnet-version: '8.0.100'

      - name: Pack FirstClassErrors (with bundled analyzer) into the local feed
        # Produce the real .nupkg a consumer restores — the analyzer is included at analyzers/dotnet/cs by
        # FirstClassErrors.csproj's _AddAnalyzerToPackage target — and drop it in the local feed that
        # FloorCheck's nuget.config points at. Packing (not ProjectReference) is what makes this job also
        # validate the packaging path: a broken analyzers/dotnet/cs would leave the analyzer absent and the
        # "prove the analyzer loaded" step below would fail. Run from tools/floor-check so the nested
        # global.json selects the floor SDK here too. -p:Version MUST match FloorCheck.csproj's PackageReference.
        working-directory: tools/floor-check
        run: dotnet pack ../../FirstClassErrors/FirstClassErrors.csproj -c Release -p:Version=1.0.0-floorcheck -o ./local-feed

      - name: Build on the floor SDK
        # tools/floor-check/global.json selects the .NET 8 (Roslyn 4.8) SDK: the repo-root global.json pins
        # .NET 10 and rollForward: latestFeature never rolls down to a lower major, so building from this
        # directory is what makes the CLI pick the nested pin. ReportAnalyzer AND detailed verbosity are both
        # required for Roslyn's per-analyzer table to reach the log (it is silent at default verbosity);
        # --no-incremental guarantees a real compilation so that table is always produced. The (verbose) log
        # is echoed only on failure to keep a green run readable.
        working-directory: tools/floor-check
        run: dotnet build -c Release --no-incremental -p:ReportAnalyzer=true -v detailed > build.log 2>&1 || { cat build.log; exit 1; }

      - name: Prove the analyzer actually loaded
        # A never-loaded analyzer would otherwise leave the build green: CS8032 is emitted only when loading
        # is attempted. The assembly name alone appears in ordinary build lines ("FirstClassErrors.Analyzers ->
        # ...dll" / paths), so match a fully-qualified analyzer *type* (`...<Name>Analyzer`): it appears only in
        # the ReportAnalyzer table, i.e. only if the analyzer really loaded and ran. The pattern deliberately
        # does not depend on the trailing diagnostic-id list, which is a Roslyn-version formatting detail.
        working-directory: tools/floor-check
        run: grep -qE 'FirstClassErrors\.Analyzers\.[A-Za-z]+Analyzer' build.log
